import { extendTheme } from "@chakra-ui/react";
import space from "@personal/app/theme/space";
import colors from "@personal/app/theme/colors";

export const theme = extendTheme({
  colors,
  space,
});
